---
import { buildUrl } from "../../utils/paths";

interface Props {
    paragraphs?: string[];
    imagePath?: string;
}

const defaultParagraphs = ["With 2.5+ years shipping software, I build software for systems that move data reliably, reduce manual work, and stay understandable as requirements change.", "Most of my recent work has been in backend services, agentic AI workflows and some hobby research.", "I have a Master's degree in Computer Science from University of California, Riverside."];

const { paragraphs = defaultParagraphs, imagePath = buildUrl("mumei.webp") } = Astro.props;
---

<section id="about" class="section-shell section-block section-transition">
    <div class="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[minmax(15rem,0.7fr)_minmax(0,1.3fr)] lg:items-start">
        <div class="about-media">
            <div class="about-image-wrap">
                <img src={imagePath} alt="Arnav Panigrahi" class="about-image" loading="lazy" />
            </div>
        </div>

        <div>
            <p class="section-kicker">About</p>
            <h2 class="section-title">I build stuff</h2>
            <div class="content-stack">
                {paragraphs.map((paragraph) => <p class="body-copy">{paragraph}</p>)}
            </div>

            <div class="about-facts">
                <div class="about-fact">
                    <p class="panel-label">What I optimize for</p>
                    <p class="body-copy-sm">Simple interfaces, reliable pipelines, and maintainable implementation.</p>
                </div>
                <div class="about-fact">
                    <p class="panel-label">How I work</p>
                    <p class="body-copy-sm">Small decisions. Clear systems. Easy maintenance.</p>
                </div>
            </div>
        </div>
    </div>
</section>
